experiment: nat_only
data:
  train_dir: data/natural
  val_dir: data/natural_val
  test_dir: data/natural_test
  classes: [brick, glass, concrete, metal, vegetation]
model:
  arch: resnet18
  pretrained: true
save_dir: results/nat
